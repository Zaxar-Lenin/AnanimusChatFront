export enum Routers {
    LogIn = "/logIn",
    Users  = '/users',
    Messages  = '/messages',
    addMessage  = '/addMes',
}

export const apiAuth = "/api/auth"
export const apiMessage = "/api/message"

